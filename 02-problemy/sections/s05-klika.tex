\section{Problém kliky v~grafu}\label{sec:klika}

Jako poslední si ukážeme ještě jeden typ problému,~který (jak uvidíme) přímo souvisí s~problémem nezávislé množiny. Nejdříve si však připomeňme některé důležité termíny z~teorie grafů. Jako první si definujeme tzv. \emph{kliku}.
\begin{definition}[Klika]
    Nechť je dán neorientovaný graf $G=(V,E)$. Množinu $K\subseteq V$ nazveme \emph{klikou},~pokud jsou každé dva různé vrcholy z~$K$ spojeny hranou.
\end{definition}
\begin{example}
    Příklady klik v~grafech $G_1$ a~$G_2$,~viz obrázky \ref{subfig:priklad_klika1} a~\ref{subfig:priklad_klika2}.
    \begin{figure}[h]
        \centering
        \begin{subfigure}{7cm}
            \centering
            \input{02-problemy/images/ch02_klika1.tex}
            \caption{Klika v~grafu $G_1$.}
            \label{subfig:priklad_klika1}
        \end{subfigure}
        \qquad
        \begin{subfigure}{7cm}
            \centering
            \input{02-problemy/images/ch02_klika2.tex}
            \caption{Klika v~grafu $G_2$.}
            \label{subfig:priklad_klika2}
        \end{subfigure}
    \end{figure}
\end{example}
Podobně jako u~nezávislé množiny,~i~zde platí,~že každý neprázdný graf jistě obsahuje kliku (např. samostatný vrchol je z~definice klika). Bude nás tedy opět zajímat,~zda v~zadaném grafu existuje klika určité velikosti.

\problem{Problém kliky v~grafu (Klika)}{Graf $G=(V,E)$ a~číslo $k\in\N$.}{$1$,~pokud v~$G$ existuje klika velikosti alespoň $k$,~jinak $0$.}

Nyní si ukážeme,~že ve skutečnosti problém kliky je ekvivalentní s~problémem nezávislé množiny. K~tomu si však definujeme ještě jeden termín.
\begin{definition}[Doplněk grafu]
    Je-li $G=(V,E)$ jednoduchý neorientovaný graf,~pak jeho \emph{doplňkem} je graf $\overline{G}=(V,\overline{E})$,~kde pro každé dva různé vrcholy $u,v\in V$ platí
    \[\set{u,v}\in\overline{E}\iff\set{u,v}\notin E.\]
\end{definition}
Co tato definice vlastně říká? Jednoduše to,~že doplněk grafu $G$ obsahuje právě ty hrany,~které v~původním grafu chybí. Podívejme se na příklady.
\begin{example}
    Příklady doplňků grafů,~viz obrázky \ref{fig:doplnek_grafu1} a~\ref{fig:doplnek_grafu2}.
    \begin{figure}[h]
        \centering
        \input{02-problemy/images/ch02_doplnek_grafu1.tex}
        \caption{Doplněk grafu na čtyřech vrcholech.}
        \label{fig:doplnek_grafu1}
    \end{figure}
    \begin{figure}[h]
        \centering
        \input{02-problemy/images/ch02_doplnek_grafu2.tex}
        \caption{Doplněk prasátkového grafu.}
        \label{fig:doplnek_grafu2}
    \end{figure}
\end{example}

\subsection{Převod Klika na NzMna}\label{subsec:prevod_klika_nzmna}

Jako poslední se zamyslíme,~jaká je tedy souvislost mezi problémem nezávislé množiny a~kliky v~grafu. Předpokládejme,~že máme zadaný graf $G$ a~číslo $k\in\N$. Chceme sestrojit graf $G^\prime$ a~číslo $k^\prime$ takové,~že v~$G$ existuje klika velikosti alespoň $k$ právě tehdy,~když v~novém grafu $G^\prime$ existuje nezávislá množina velikosti alespoň $k^\prime$. To zařídíme velice jednoduše.

Pro daný graf $G$ sestrojíme jeho doplněk $\overline{G}$. Pokud je $K$ klikou v~$G$,~pak každé dva různé vrcholy z~$K$ jsou v~$G$ spojeny hranou,~a~proto v~$\overline{G}$ spojeny nejsou. Množina $K$ je tedy nezávislá v~$\overline{G}$. Stejný argument funguje i~obráceně.

Tedy celkově stačí položit $G^\prime=\overline{G}$ a~$k^\prime=k$ a~jsme hotovi. Opačný převod provedeme naprosto stejně.
